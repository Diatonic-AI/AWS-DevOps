{
  "name": "client-billing-costs",
  "version": "1.0.0",
  "description": "Lambda function to retrieve client billing costs from AWS Cost Explorer",
  "main": "index.js",
  "scripts": {
    "deploy": "zip -r function.zip . && aws lambda update-function-code --function-name client-billing-costs --zip-file fileb://function.zip"
  },
  "dependencies": {
    "@aws-sdk/client-cost-explorer": "^3.x",
    "@aws-sdk/client-cloudwatch": "^3.x",
    "@aws-sdk/client-dynamodb": "^3.x",
    "@aws-sdk/lib-dynamodb": "^3.x"
  }
}
